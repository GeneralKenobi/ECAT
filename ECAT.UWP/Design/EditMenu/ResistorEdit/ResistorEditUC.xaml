<UserControl
    x:Class="ECAT.UWP.ResistorEditUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ECAT.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

	<StackPanel>

		<!-- Resistance label -->
		<TextBlock Style="{StaticResource WhiteTextBlockStyle}"
			FontSize="{StaticResource FontSizeBig}"
			Text="{Binding ResistanceEditHeader, Mode=OneTime}"
			Margin="0 0 0 10"/>

		<!-- TextBox with the resistance settings -->
		<TextBox Style="{StaticResource SideMenuTextBoxStyleUnder8}"
			Text="{Binding Resistance, Mode=TwoWay}"			
			Grid.ColumnSpan="2"/>

	</StackPanel>

</UserControl>