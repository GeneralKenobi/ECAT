<UserControl
    x:Class="ECAT.UWP.SweepVoltageSourceEditUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ECAT.UWP"
    xmlns:converters="using:UWPEnhanced.ValueConverters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

	<UserControl.Resources>
		<converters:DoubleToSIStringConverter x:Key="SIStringConverter"/>
	</UserControl.Resources>

	<StackPanel>

		<!-- Start Frequency -->
		<TextBlock Style="{StaticResource WhiteTextBlockStyle}"
			FontSize="{StaticResource FontSizeBig}"
			Text="{Binding StartFrequencyHeader, Mode=OneTime}"
			Margin="0 0 0 10"/>

		<!-- TextBox with the start frequency settings -->
		<TextBox Style="{StaticResource SideMenuTextBoxStyleUnder8}"
			Text="{Binding StartFrequency, Mode=TwoWay, Converter={StaticResource SIStringConverter}}"/>

		<!-- End Frequency -->
		<TextBlock Style="{StaticResource WhiteTextBlockStyle}"
			FontSize="{StaticResource FontSizeBig}"
			Text="{Binding EndFrequencyHeader, Mode=OneTime}"
			Margin="0 0 0 10"/>

		<!-- TextBox with the end frequency settings -->
		<TextBox Style="{StaticResource SideMenuTextBoxStyleUnder8}"
			Text="{Binding EndFrequency, Mode=TwoWay, Converter={StaticResource SIStringConverter}}"/>

	</StackPanel>

</UserControl>
